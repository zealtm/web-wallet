const initialState = {
    reload: false,
};
const invite = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return {
              ...state
            };
        }
    }
};
export default invite;